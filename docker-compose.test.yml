services:
  app-under-test:
    build: .
    command: python -m pytest test_app.py -v
    depends_on:
      - test-db

  test-db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: testpass
