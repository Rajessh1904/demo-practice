name: CI-CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login only on push to main (or PRs if needed)
      - name: Log in to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build image locally for testing and security scans
      - name: Build local image for testing
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true # Loads image into local Docker daemon so Trivy & Docker Compose can see it
          tags: |
            proedge/app:${{ github.sha }}
            proedge/app:latest

      - name: Run unit + integration tests
        run: docker compose -f docker-compose.test.yml up --build --abort-on-container-exit

      - name: Security scan (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: proedge/app:${{ github.sha }}
          exit-code: "0"
          severity: "CRITICAL"

      - name: Generate SBOM
        run: |
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy image --format cyclonedx --output sbom.json proedge/app:${{ github.sha }}

      # Push images to registry only on main branch
      - name: Push image to Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            proedge/app:${{ github.sha }}
            proedge/app:latest

  deploy-dev:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to dev environment automatically..."

  deploy-staging:
    needs: deploy-dev
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: staging # Requires manual approval in GitHub repo settings
    steps:
      - run: echo "Deploying to staging after manual approval..."

  deploy-prod:
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - run: echo "Deploying to production!"